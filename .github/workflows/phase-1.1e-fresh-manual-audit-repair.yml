name: Phase 1.1E Fresh Manual-Audit Repair

on:
  push:
    branches:
      - phase-1.1e-canonical-guidance
    paths:
      - '.github/workflows/phase-1.1e-fresh-manual-audit-repair.yml'
      - '.github/scripts/phase_1_1e_fresh_manual_audit_repair.py'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: phase-1.1e-canonical-guidance
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install package and test dependencies
        run: python -m pip install -e '.[test]'

      - name: Verify SOE-1.0.0 remains frozen
        shell: bash
        run: |
          git fetch origin main --depth=1
          test "$(git rev-parse HEAD:config/soe_v1_0_rules.yaml)" = "$(git rev-parse origin/main:config/soe_v1_0_rules.yaml)"
          echo "frozen_rules_blob_sha=$(git rev-parse HEAD:config/soe_v1_0_rules.yaml)"

      - name: Apply fresh manual-audit semantic repair
        run: python .github/scripts/phase_1_1e_fresh_manual_audit_repair.py

      - name: Run targeted guidance regressions
        run: |
          python -m pytest backend/tests/test_guidance_manual_audit_regressions.py \
            backend/tests/test_guidance_raw_canonical_extractor.py \
            backend/tests/test_canonical_shadow_guidance_integration.py

      - name: Run full deterministic suite
        env:
          PROVIDER_NAME: fixture
          DATABASE_URL: sqlite+pysqlite:///./soe_phase_1_1e_ci.db
        run: python -m pytest

      - name: Commit validated repair
        shell: bash
        run: |
          git status --short
          test "$(git rev-parse HEAD:config/soe_v1_0_rules.yaml)" = "$(git rev-parse origin/main:config/soe_v1_0_rules.yaml)"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add backend/app/services/guidance_raw_canonical_extractor.py backend/tests/test_guidance_manual_audit_regressions.py
          if git diff --cached --quiet; then
            echo 'No repair changes to commit.'
            exit 0
          fi
          git commit -m 'Phase 1.1E: harden guidance after fresh manual audit'
          git push origin HEAD:phase-1.1e-canonical-guidance
